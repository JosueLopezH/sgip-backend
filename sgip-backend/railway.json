{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd sgip-backend && npx prisma migrate deploy && npx prisma db seed && node dist/src/main.js",
    "restartPolicyType": "ON_FAILURE"
  }
}