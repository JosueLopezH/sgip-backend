{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd sgip-backend && npm install && npm run build && npx prisma generate && npx prisma migrate deploy && node dist/src/main.js",
    "restartPolicyType": "ON_FAILURE"
  }
}